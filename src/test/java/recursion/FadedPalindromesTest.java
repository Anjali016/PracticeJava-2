package recursion;

import org.junit.Test;

import static org.junit.Assert.assertEquals;

public class FadedPalindromesTest {

    @Test
    public void test1() {
        FadedPalindromes fadedPalindromes = new FadedPalindromes();
       // String str = "a.b";
       // assertEquals("-1", fadedPalindromes.fadedPalindrome(str, 0, str.length() - 1));

    }
}
